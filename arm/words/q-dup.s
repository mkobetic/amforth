@ -----------------------------------------------------------------------------
  CODEWORD "?dup", QDUP /* ( x -- x x | 0 ) duplicate TOS if it is not zero */
@ -----------------------------------------------------------------------------
  cmp tos, #0
  beq 1f
  savetos
1:
  NEXT
END QDUP
