
STACK:
    dup ?dup swap over drop rot -rot tuck nip pick 
    2dup  2swap 2over 2drop 2rot 2nip 
    depth ?stack sp0 sp sp! sp@
    rdrop rdepth rp rp! rp@ r@ r> >r nr> n>r 2r> 2>r 2r@
COMPARE:
    umin umax max min = <> > < <= >= 0< 0<> 0= 0> within
    u> u< u<= u>=
MATH:
    * + - / mod /mod */ */mod u/mod
    1- 1+ 2- 2+ 2/ 2*
    not or and xor invert lshift rshift ashift abs negate
MIXED:
    m* m+ um* um/mod sm/rem m/mod
DOUBLE:
    s>d d= d0= d0< d> d<
    d- d+ d/ d/mod ud* ud/mod
    d2/ d2* dnegate dabs
MEMORY:
    ! @ h! h@ c! c@ +! fill move
    2@ 2! char+ chars cells cell cell- cell+
    !i
    align here allot aligned (aligned)
    valign vhere vallot vp vp.max vp0
    dalign dhere  dallot (dallot) dp dp0.ram dp.ram dp.flash
    ealign ehere eallot ep 
    ram++ ram+ ram >ram >flash
    up! up@
STRING:
    s, ." s" sub-string? number >number digit? toupper
    /string parse parse-name cskip cscan
    search bounds compare count
DICTIONARY:
    ' c, , h, (c,) (,) chkdalign reveal newest latest 
    value value~ to (to) is variable variable~ constant constant~ :noname header
    find find-xt forget words  interpret execute
    nfa>string nfa>xt lfa>ffa ffa>nfa xt>string >body name>flags ffa>string ffa2cfa ffa>lfa xt>ffa xt>nfa
WORDLIST:
    wordlist forth-wordlist ram-wordlist core-wordlist environment rv-wordlist arm-wordlist
    get-current  wlscope search-wordlist current show-wordlist traverse-wordlist
    set-current previous set-order get-order order only w1  order.table  (order) core forth order.forth order.core order.only cfg-order top> >top top
COMPILER:
    : ; ;i :~ ( \# \  recurse postpone immediate state
    create (create) does> (dodoes) char [ ] ['] [compile] [char] 
    compile xliteral literal 2literal sliteral
    Udefer! Udefer@ defer! defer@ defer~ defer
    evaluate (evaluate) evaluate.source evaluate.str evaluate.strlen
FLOW:
    ?abort abort" abort trap! throw catch handler exit (exit) (exiti)  
    repeat again else if while until then begin ahead
    +loop  loop  endloop do ?do (qdocheck) j i unloop leave >l l> lp lp0 
IO:
    serial-emit? serial-emit serial-key? serial-key emit? serial-emit-pause emit key? serial-key-pause key
    tib #tib >in source-tib source accept refill-tib refill ?crlf bs
OUTPUT:
    . .k .s ud.r ud. u. d. d. d.r r.. r. hex. 8x. 2x. x. .8hex
    spaces space bl cr base
    itype type pad binary hex decimal 
    sign #> #s # <# hold hld
SYSTEM:
    cold warm init-ram turnkey appl-turnkey quit pause
    ms us ver rev-info build-info 
    pv.do pv.init vaddr pv.store pvp
    pvflash.size pvflash.page pvflash.cell pvflash.erased pvflash.erase @pvf !pvf
    pvarena.size pvarena.init pvarena.swap pvarena.erase pvarena pvarena.dormant pvarena1 pvarena2
TOOLS:
    debug || dump vdump break
OTHER:
    uart-init
    cacheflush inflash? flashpageerase c!flash w!flash !flash memmode
    pp     ?ascii  -private +private public? 
    (forget) forget.prior  trap.base m4fix mtog mclr mset
    -cloak +cloak cloak?
    public private flag.private flag.cloaked flag.table flag.init flag.defer flag.value flag.pvalue flag.immed flag.con flag.colon flag.dvar flag.var flag.code flag.private? flag.header
    map-stack set-base bases .ready .ok .input .error first-run
    false true -1 4 2 1 0
    cfg-recognizer forth-recognizer split rectype-split rec-split rec-num rectype-dnum rectype-num rectype-xt rec-find rectype-null recognize
    empty eeprom.empty? eeprom.init eeprom.warm
    flash.low ~!i !i std.erase std.unlock                         